import {Package} from './package';
import {PackageTypeEnum} from './package-type-enum';

export class MostPopularPackage extends Package {

    // getPackageType(): PackageTypeEnum {
    //     return PackageTypeEnum.MostPopular;
    // }
}